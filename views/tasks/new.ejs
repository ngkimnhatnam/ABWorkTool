<%- include("../partials/header") %>

<div class="container">
	<div class="row">
		<div style="width: 30%; margin: 25px auto;">
			<h1 style="text-align: center">
				Create new task 
			</h1>
			<form action="/task" method="POST">
				<div class="form-group">
					<input class="form-control" type="text" name="date" placeholder="Date">
				</div>
				<div class="form-group">
					<div class="input-group mb-3">
					  <div class="input-group-prepend">
						<label class="input-group-text" for="inputGroupSelect01">Worker</label>
					  </div>
					  <select class="custom-select" id="inputGroupSelect01" name="worker">
						<option value="">Assign new worker</option>
						<% users.forEach(function(user){ %>
							<option value="<%=user.username%>" ><%=user.username%></option>
						<% }) %>
					  </select>
					</div>
				</div>
				<div class="form-group">
					<div class="input-group">
					  <select class="custom-select" id="inputGroupSelect04" aria-label="Example select with button addon" name="unit">
						<option value="">Choose the unit</option>
						<% units.forEach(function(unit){ %>
							<option value="<%=unit.name%>"><%=unit.name%></option>
						<% }) %>
						
					  </select>
					  <div class="input-group-append">
						<button class="btn btn-outline-secondary" type="button">Add unit</button>
					  </div>
					</div>
				</div>
				<div class="form-group">
					<input class="form-control" type="text" name="sidenote" placeholder="Add sidenote here">
				</div>
				<div class="form-group">
					<button class="btn btn-lg btn-primary btn-block">
						Add
					</button>
				</div>
		</form>
			<a href="/task">Back to tasks</a>
		</div>
		
		
	</div>
	
</div>












<%- include("../partials/footer") %>