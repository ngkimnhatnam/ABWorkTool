<!DOCTYPE html>
<html>
	<head>
		<!-- Required meta tags -->
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>AirBnb WorkTool</title>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
		<link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css">
		<link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
	</head>
	<body>
		<div class="container">	
			<nav class="navbar navbar-light bg-light" style="padding: 3px 10px 3px 10px;" id="jumbotron">
				<a class="navbar-brand"></a>
				<ul class="navbar-nav">		
					<li class="nav-item"><%=date%></li>
					<% if(currentUser&&currentUser.isManager){%>
				  	<li class="nav-item">Signed in as Admin <%= currentUser.nickname%></li>
					<% } else {%>
					<li class="nav-item">Signed in as <%= currentUser.nickname%></li>
					<% } %>
					<li class="nav-item nav-link" id="logout"><a href="/logout" >Log out</a></li>
				</ul>
			</nav>
			<header class="jumbotron" id="jumbo">
				<h1 id="pageName"><a class="navbar-brand" href="/" id="pageName">AirBnb Workspace</a></h1>
			</header>
			<div class="container" id="flash_mes">
			<% if(error && error.length > 0){ %>
				<div class="alert alert-danger" role="alert"><%= error %></div>
			<% } %>
			<% if(success && success.length >0){ %>
				<div class="alert alert-success" role="alert"><%= success %></div>
			<% } %>
			</div>
				